import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"], variable: "--font-inter", display: "swap" });

export const metadata: Metadata = {
  title: "RegimeGate — Adaptive Fusion Controller",
  description:
    "A <50k-parameter gate that learns where each forecaster should be trusted and re-allocates fusion weights live, conditioned on the demand regime. Validated on real Walmart M5.",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={inter.variable}>
      <body className="font-sans antialiased">{children}</body>
    </html>
  );
}
